# 面试准备 · 双系统 VLA 仓储补货机器人项目

> 覆盖：项目本身 Q&A + 周边会被问到的其他方面（ML/DL 基础、VLA 领域、相关算法、行为题）。
> 标注约定：✅ 可自信讲　⚠️ 要诚实承认局限　🔥 高频追问/陷阱题

## 目录
- **零、核心问题精讲（小林 coding 格式重写）** ← 新增
- 一、项目整体与架构
- 二、G0 双系统原理
- 三、VLA 模型细节（System 1）
- 四、跨本体微调
- 五、分层部署与 VLM 规划
- 六、闭环完成判断（核心亮点）
- 七、数据与评估
- 八、局限与改进
- 九、其他方面：ML/DL 基础
- 十、其他方面：Transformer 与注意力
- 十一、其他方面：扩散与流匹配
- 十二、其他方面：多模态/VLM
- 十三、其他方面：模仿学习与 RL
- 十四、其他方面：VLA 领域横向对比
- 十五、其他方面：LoRA/PEFT 深挖
- 十六、其他方面：机器人学基础
- 十七、行为面试与项目软问题
- 十八、其他方面：经典 CV 模型与基础
- 十九、其他方面：经典 NLP / LLM 基础
- 二十、其他方面：优化与训练
- 二十一、其他方面：评估指标
- 二十二、其他方面：工程与系统

---

## 零、核心问题精讲（小林 coding 格式重写）

> 本节挑选项目最核心、最容易被深挖的 12 个问题，按【面试官会怎么问（含追问）】→【简要回答】→【详细解析（原理 + 代码 + 设计动机）】→【现场话术】→【追问链】→【考察什么 / 陷阱扣分】→【面试总结】的结构重写。
> 后续章节保留原 Q&A 速查形式，作为知识面覆盖；面试当天主练本节。

### 0.1【开场必答】Q1 一句话介绍项目 🧭

👔 面试官：用一句话介绍一下你这个项目。

🙋‍♂️ 我：呃，就是用 G0Plus 模型做一个超市补货机器人，让它能听指令去货架拿东西补货……

👔 面试官：太散了。一句话要能压出"技术路线 + 任务 + 我的贡献"三层信息，不然我听不出你到底做了什么。再组织一下。

这道题是面试的开场，**一句话讲不清 = 后面再难翻**。核心是把它压成"技术路线 + 任务场景 + 我的三个贡献"。

#### 💡 简要回答

把预训练 G0Plus 双系统 VLA 通过 LoRA 跨本体微调迁移到 Fetch 机器人，并搭建 VLM 分层规划 + 传感器触发/VLM 裁决的闭环完成判断，实现仓储补货的长程移动操作。三个贡献：①跨本体 LoRA 微调；②事件触发的闭环完成判断；③闭环评估管线 + grounding 失效诊断。

#### 📝 详细解析

**为什么这一句话要这么组织？**

面试官听一句话要拿到三个信息：
1. **技术路线**：预训练 VLA + LoRA 跨本体微调 + 双系统分层 + 闭环判断——一句话压住四个关键词，证明你用的是当前主流路线，不是自己拍脑袋。
2. **任务场景**：仓储补货的长程移动操作——"长程"暗示任务跨千步、分钟级，这是分层架构的动机；"移动操作"暗示既有导航又有操作，覆盖面广。
3. **我的贡献**：跨本体微调、闭环判断、评估诊断——把"我做了什么"和"框架本身有什么"分开，避免被追问"这是你做的还是论文里现成的"时翻车。

**为什么强调"预训练 G0Plus + LoRA 跨本体"？**

这是当前 VLA 领域的主流范式：不在小数据上从头训，而是用大规模预训练 VLA 当先验，再用少量目标本体数据 LoRA 微调。强调这一点等于告诉面试官"我知道主流怎么玩，不是闭门造车"。

**为什么把"闭环完成判断"单列？**

因为这是 sim2real 的核心 gap——仿真有 ground-truth done，真机没有。我把它当独立贡献讲，等于告诉面试官"我抓住了真机部署的真问题，不是只跑通 demo"。

#### 🎤 现场话术（30 秒）

"这个项目是把预训练的 G0Plus 双系统 VLA，通过 LoRA 跨本体微调迁移到 Fetch 机器人，搭一个 VLM 分层规划加传感器触发/VLM 裁决的闭环完成判断，做仓储补货的长程移动操作。我三个核心工作：一是跨本体 LoRA 微调把 G0Plus 从 R1Lite 迁到 Fetch；二是设计了事件触发的闭环完成判断解决 sim2real 的 done 信号 gap；三是搭了闭环评估管线并诊断出模型的语言 grounding 失效问题——定位到模型走了视觉捷径不读指令。"

#### 追问链

- 追"为什么用 G0Plus 不用 OpenVLA？"→ G0Plus 是双系统 + 大规模真实移动操作数据 + 跨本体设计，正好对上仓储补货长程任务；OpenVLA 偏单体桌面操作。
- 追"LoRA 跨本体具体怎么做的？"→ 见 0.5 节。
- 追"grounding 失效怎么诊断的？"→ 见 0.9 节。
- 追"成功率多少？"→ ⚠️ 诚实：架构链路端到端跑通，闭环成功率还在评估中，不编数字。

#### 考察什么 / 陷阱扣分

**考察什么**：①能不能压成一句话讲清技术路线 ②区分"框架现成的"和"我做的" ③主动抛亮点钩子（grounding 诊断）

**陷阱/扣分**：❌ 讲成"用 G0Plus 做了个机器人"→ 没贡献 ❌ 把论文设计说成自己的 → 追问必翻 ❌ 一口气讲 5 分钟把弹药打光 → 没留追问钩子

#### 🎯 面试总结

这题考的是"能不能 30 秒讲清自己做了什么"。三层：第一层**技术路线**（G0Plus+LoRA+双系统+闭环），第二层**任务场景**（仓储补货长程移动操作），第三层**我的贡献**（跨本体+闭环判断+grounding 诊断）。第三层是分水岭——能区分"框架现成的"和"我做的"，才算真懂项目。

---

### 0.2【架构核心】Q2 为什么用分层架构，不用端到端长程 VLA？🔥 🧭

👔 面试官：你为什么用分层架构，不让 VLA 端到端从指令直接出到补货完成的整条轨迹？

🙋‍♂️ 我：因为端到端太难训了，数据不够。

👔 面试官：难在哪？分层到底解决了什么？如果你数据足够多，端到端不就行了？

🙋‍♂️ 我：呃……端到端需要的数据量太大，分层可以省数据……

👔 面试官：省数据只是表象。分层的真正价值是"组合泛化"——复合任务的组合交给 VLM 零样本，只有原子技能才需要数据训练。讲不清这个，等于不知道自己为什么分层。回去想清楚再来。

这题是架构决策的王牌，**讲不清"组合泛化"和"原子技能 vs 复合任务"的区分，等于分层白做了**。

#### 💡 简要回答

长程任务跨千步、分钟级，端到端难学难泛化。分层后复合任务的"组合"交给 VLM 零样本完成，只有原子操作技能才需要数据训练 VLA。加新复合任务只改提示词，不重训 VLA。

#### 📝 详细解析

**背景：端到端 VLA 的根本困难**

端到端 VLA（如 RT-1、OpenVLA）是单网络：图像+指令直接出动作。问题是长程任务（如"把可乐从货架搬到篮子"）跨千步、分钟级，端到端要学的映射是"整条指令 → 整条轨迹"，这个映射：
1. **数据需求爆炸**：要覆盖所有可能的复合任务组合，组合空间是原子技能数的指数级。
2. **泛化困难**：训练集没见过的复合任务，端到端没法零样本组合。
3. **长程误差累积**：千步轨迹一步错步步错，val loss 低不代表任务成。

**分层怎么解决**

分层把任务拆成两层：
- **复合任务组合**（"先去 A 货架拿可乐，再放到篮子，再去 B 货架拿雪碧"）→ 交给 VLM 零样本规划。VLM 靠预训练语义理解就能拆子任务，不需要训练数据。
- **原子操作技能**（pick_to_basket、restock、pick_from_floor）→ 才需要数据训练 VLA。原子技能数量少、每个技能轨迹短（几十秒），数据需求可控。

**组合泛化是核心**

分层真正的价值不是"省数据"，是**组合泛化**——训练时只见过 pick_to_basket(可乐) 和 pick_to_basket(雪碧)，部署时 VLM 能零样本组合出"先拿可乐再拿雪碧"的新复合任务，VLA 只管执行原子技能。这等于把"指数级组合空间"压成"线性级原子技能空间"。

**加新复合任务的成本**

端到端：要采集新复合任务的整条轨迹数据，重训。分层：只改 VLM 提示词，VLA 不动。这是工程上的巨大差异——分层后加新任务像写 prompt，端到端像重训模型。

**如果数据足够多，端到端不就行了？**

理论上是的，但"足够多"是指数级，现实中永远不够。而且即使数据够，端到端的长程误差累积问题依然在——分层把长程拆成短原子段，每段误差不累积。所以即使数据充足，分层在工程上仍更可控。

#### 🎤 现场话术（40 秒）

"长程任务跨千步、分钟级，端到端 VLA 要学'整条指令到整条轨迹'的映射，数据需求是指数级——要覆盖所有复合任务组合。分层把任务拆两层：复合任务的组合交给 VLM 零样本规划，靠预训练语义理解就能拆子任务，不需要训练数据；只有原子操作技能才需要数据训练 VLA，原子技能数量少、轨迹短，数据需求可控。真正的价值是组合泛化——训练时只见过原子技能，部署时 VLM 能零样本组合出新复合任务，VLA 只管执行。加新复合任务只改提示词不重训 VLA，这是工程上的核心差异。"

#### 追问链

- 追"如果数据足够多，端到端不就行了？"→ 理论上是的，但"足够多"是指数级现实中永远不够；且长程误差累积问题分层更可控。
- 追"VLM 规划错了怎么办？"→ VLM 输出 JSON 子任务序列经 `_parse` 校验，格式错直接失败；语义错靠闭环完成判断兜底（见 0.7 节）。
- 追"原子技能怎么界定？"→ 按"一个连续动作块+一个完成判断点"界定，如 pick_to_basket 是"伸手→抓→抬→移→放篮子"一个完整操作。
- 追"分层和端到端的边界在哪？"→ 分层是显式拆子任务+独立完成判断；端到端是单网络出整条轨迹。中间态（如 RT-2 用 LLM 出动作 token）算端到端。

#### 考察什么 / 陷阱扣分

**考察什么**：①端到端的根本困难（数据爆炸+长程累积） ②分层的核心价值（组合泛化） ③原子技能 vs 复合任务的区分 ④加新任务的成本差异

**陷阱/扣分**：❌ 只说"省数据"不讲组合泛化 → 没抓核心 ❌ 把分层说成"工程方便" → 是设计层面的决策 ❌ 答不出"数据足够多端到端行不行" → 没想透边界

#### 🎯 面试总结

这题考的是"架构决策的动机"。三层：第一层**端到端的困难**（数据指数级+长程累积），第二层**分层的解法**（复合任务交 VLM 零样本+原子技能训 VLA），第三层**组合泛化的核心价值**（训练原子技能，部署零样本组合）。第三层是分水岭——能讲清"组合泛化把指数级压成线性级"，才算真懂为什么分层。

---

### 0.3【双系统原理】Q4 双系统怎么运行？频率？🔥 🧭

👔 面试官：你老说双系统，两个系统到底怎么协作？频率多少？

🙋‍♂️ 我：System 1 出动作，System 2 出子任务，两个一起跑。

👔 面试官：频率呢？是同步还是异步？System 2 在 System 1 执行的时候干嘛？

🙋‍♂️ 我：呃……应该是同步的吧，System 2 等 System 1 执行完再规划下一个……

👔 面试官：错了。G0 的双系统是**异步**的——System 2 持续 2Hz 重规划"下一个子任务"，System 1 在当前子任务下 15Hz 执行。两系统不互相等。讲不清异步，等于不懂双系统的高效在哪。

这题考的是"双系统怎么协作"，**讲不清异步和频率金字塔，等于不懂双系统为什么高效**。

#### 💡 简要回答

Figure 1 频率金字塔：200Hz 底层控制 / 15Hz System1 VLA 出 action chunk / 2Hz System2 VLM 出子任务。两系统**异步**：System2 持续 2Hz 重规划"下一个子任务"，System1 在当前子任务下 15Hz 执行。

#### 📝 详细解析

**频率金字塔的三层**

G0 的双系统是三层频率金字塔：
- **200Hz 底层控制**：PD 控制器跟踪关节位置，纯反馈控制，无学习。
- **15Hz System1 VLA**：图像+指令+状态 → action chunk（一次出 32 步未来动作），这是 G0Plus 执行网络。15Hz 意味着每 66ms 出一次动作块。
- **2Hz System2 VLM**：头部图+历史观测+任务名 → 子任务语言。2Hz 意味着每 500ms 重规划一次"下一个子任务"。

**为什么是异步不是同步？**

同步意味着 System2 等 System1 执行完当前子任务再规划下一个——这会让 System2 大部分时间空转（System1 执行一个子任务要几十秒），且 System1 执行完才规划会造成"规划空窗期"。

异步意味着 System2 持续 2Hz 重规划"下一个子任务"，不等 System1 执行完。System1 在当前子任务下 15Hz 执行，System2 同时在后台算"下一个该干什么"。当 System1 执行完当前子任务，System2 早就准备好了下一个，无缝切换。

**异步的核心好处**

1. **无规划空窗期**：System2 提前算好下一个子任务，System1 执行完立即切换。
2. **System2 能持续观察世界**：2Hz 重规划意味着 System2 持续看世界，如果世界变了（如物品掉地），能及时调整下一个子任务。
3. **频率匹配任务复杂度**：规划是语义任务（慢、低频），执行是控制任务（快、高频），频率分层匹配各自需求。

**System2 怎么判断子任务完成？**

System2 不显式判断"当前子任务完成了"，而是持续决定"下一个子任务是什么"。训练时给"子任务将结束/夹爪变化"的 key frame 更高采样权重，让 System2 学会识别转换点——当它输出的"下一个子任务"和当前子任务不同时，就是转换点。

**我的项目和 G0 的差异**

G0 本地模型 2Hz 免费；我用 API 调 VLM，固定 2Hz 轮询慢且烧钱。所以我改成**事件触发**——传感器（夹爪/到位/yaw）触发才调 VLM，省调用低延迟。本质相同（都是异步），但触发方式不同。

#### 🎤 现场话术（40 秒）

"G0 的双系统是频率金字塔：200Hz 底层 PD 控制 / 15Hz System1 VLA 出 action chunk / 2Hz System2 VLM 出子任务。两系统**异步**——System2 持续 2Hz 重规划'下一个子任务'，不等 System1 执行完；System1 在当前子任务下 15Hz 执行。当 System1 执行完，System2 早就准备好下一个，无缝切换。异步的核心好处是无规划空窗期 + System2 持续观察世界能及时调整。System2 不显式判断完成，而是持续决定下一个子任务，训练时给子任务转换的 key frame 更高采样权重让它学会识别转换点。我项目和 G0 的差异：G0 本地 2Hz 免费，我用 API 改成事件触发省调用。"

#### 追问链

- 追"为什么 System2 是 2Hz 不是更高？"→ 规划是语义任务，VLM 推理慢，2Hz 是算力和延迟的平衡；更高频没必要（子任务秒级才切换）。
- 追"System2 怎么知道当前子任务完成了？"→ 不显式判断，持续决定下一个子任务；输出和当前子任务不同时就是转换点。
- 追"你为什么不用 2Hz 用事件触发？"→ G0 本地模型 2Hz 免费；我用 API 固定轮询慢且烧钱、中途 99% 返回"还在做"是浪费。事件触发省调用低延迟。
- 追"异步会不会有竞态条件？"→ 会有，System2 刚规划完下一个子任务，System1 还在执行上一个。靠"System1 执行完才切换"解决——System2 的输出是"下一个待执行"，不是"立即切换"。

#### 考察什么 / 陷阱扣分

**考察什么**：①频率金字塔（200/15/2Hz） ②异步 vs 同步的区别 ③异步的好处（无空窗+持续观察） ④System2 怎么判断完成（不显式，持续决定下一个）

**陷阱/扣分**：❌ 说成同步 → 致命概念错 ❌ 答不出为什么异步 → 没理解设计动机 ❌ 把 System2 说成"等 System1 执行完才规划" → 错

#### 🎯 面试总结

这题考的是"双系统怎么协作"。三层：第一层**频率金字塔**（200/15/2Hz），第二层**异步 vs 同步**（System2 持续重规划不等 System1），第三层**异步的好处**（无空窗+持续观察世界）。第三层是分水岭——能讲清"异步让 System2 提前算好下一个+持续观察世界能及时调整"，才算真懂双系统为什么高效。

---

### 0.4【核心亮点】Q11 VLA 内部也有 VLM，为什么不让它自判完成？🔥 🧭

👔 面试官：你用独立 VLM 判断完成，但 VLA 内部不也有 VLM 吗？为什么不让它自己判？省一个模型不是更省事？

🙋‍♂️ 我：因为 VLA 没有输出口，只输出动作。

👔 面试官：那加一个输出口不就行了？还有别的原因吗？

🙋‍♂️ 我：呃……还有就是 VLA 没被训过判断完成……

👔 面试官：三个原因都没讲全。最关键的是"generator 当自己的 verifier 有利益冲突"——生成者给自己打完成分，校准差。独立判断者信号更干净。这是 generator/verifier 分离的核心思想，讲不清等于不懂为什么独立判断。

这题是闭环判断的核心动机，**讲不清 generator/verifier 分离，等于不懂为什么独立 VLM 判断**。

#### 💡 简要回答

三点：①没有输出口（只输出动作块，无 termination 通道）；②没被训过（无完成标签）；③看不到判断所需信息（单帧+手腕特写，判断任务级完成需场景级+时序，那是 System2 的输入）。更深：让生成者给自己打完成分=generator 当自己的 verifier，校准差、有利益冲突。独立判断者信号更干净——generator/verifier 分离。

#### 📝 详细解析

**表面三层原因**

1. **没有输出口**：VLA（G0Plus 执行网络）只输出 13 维×32 步动作块，没有 termination 通道。要加完成判断输出，得改架构+加输出头+重新训练，工程上不现实。
2. **没被训过**：VLA 的训练数据是专家演示轨迹，没有"完成/未完成"的标签。要让它学会判断完成，得额外标注完成点+重新训练，数据成本高。
3. **看不到判断所需信息**：VLA 输入是 3 路相机当前帧+当前状态+指令，其中一路是手腕特写（看夹爪）。判断任务级完成（如"可乐放到篮子了吗"）需要场景级视野（看整个篮子）+ 时序信息（前后帧对比看物品到位）。VLA 的输入是单帧+手腕特写，不适合判任务级完成——那是 System2 的输入（头部图+历史观测）。

**深层原因：generator/verifier 分离**

即使解决了上面三个工程问题（加输出口、标注完成、换输入），让 VLA 自判完成仍然不对——因为这是让**生成者当自己的验证器**。

生成者（generator）的任务是"生成动作"，验证器（verifier）的任务是"判断动作执行后任务完成没"。这两个任务有利益冲突：
- 生成者倾向于"我生成的动作是对的"——它刚生成了动作，让它判"动作执行完任务成了吗"，它倾向于说"成了"（自我肯定偏差）。
- 独立验证器没有这个偏差——它没生成动作，只看结果判成败，信号更干净。

**generator/verifier gap 的理论支撑**

验证比生成容易——这是机器学习的普遍规律：
- 图像分类（验证"这张图是不是猫"）比图像生成（生成猫的图）简单。
- 判断"这段翻译对不对"比"做这段翻译"简单。
- 判断"这个动作执行完任务成了吗"比"生成这个动作"简单。

所以用一个独立的、专门做验证的模型，比让生成者自判更准。这是 G0 双系统设计的深层思想——System1 生成，System2 验证（通过持续决定下一个子任务隐式验证）。

**我的项目怎么落地**

我用独立 VLM API 当 verifier：传感器触发（夹爪/到位/yaw）→ VLM 看头部前后帧 → 只判三态（IN_PROGRESS/SUCCESS/FAIL）。VLM 没生成动作，只看结果判，信号干净。失败后是重试还是重规划，由控制器层根据"世界是否破坏剩余计划前提"决定（见 0.8 节），不塞进 verifier。

#### 🎤 现场话术（40 秒）

"三层原因：①VLA 没有输出口，只输出动作块没有 termination 通道；②没被训过，训练数据是专家演示没有完成标签；③看不到判断所需信息，VLA 输入是单帧+手腕特写，判断任务级完成需场景级视野+时序，那是 System2 的输入。但最关键的是深层原因——让 VLA 自判完成等于让生成者当自己的 verifier，有利益冲突：它刚生成动作，让它判'动作执行完任务成了吗'，倾向于自我肯定。独立验证器没这个偏差，信号更干净。这是 generator/verifier gap——验证比生成容易，所以用独立模型专门做验证更准。我用独立 VLM API 当 verifier，只判三态 IN_PROGRESS/SUCCESS/FAIL，失败后重试还是重规划由控制器层决定。"

#### 追问链

- 追"加个输出口+标注完成+换输入，VLA 自判不行吗？"→ 工程上可以，但 generator/verifier 利益冲突依然在——生成者自判有自我肯定偏差。
- 追"generator/verifier gap 有什么理论支撑？"→ 验证比生成容易是机器学习普遍规律（分类比生成简单、判翻译比做翻译简单），所以独立验证更准。
- 追"VLM 判断比 VLA 自判好在哪？"→ VLM 没生成动作，只看结果判，无利益冲突；且 VLM 输入是头部图+时序，适合判任务级完成。
- 追"VLM 判断错了怎么办？"→ 降级返回 IN_PROGRESS——宁继续不误判。失败后重试还是重规划由控制器层根据世界是否破坏剩余计划前提决定。

#### 考察什么 / 陷阱扣分

**考察什么**：①表面三层原因（无输出口/没训过/看不到） ②深层 generator/verifier 分离思想 ③generator/verifier gap 的理论直觉 ④能落回项目（独立 VLM 三态判断）

**陷阱/扣分**：❌ 只讲表面三层不讲 generator/verifier → 没抓核心 ❌ 把 generator/verifier 说成"两个模型分工" → 是利益冲突问题不是分工问题 ❌ 答不出"验证比生成容易"的直觉 → 没理论支撑

#### 🎯 面试总结

这题考的是"为什么独立判断"。三层：第一层**表面原因**（无输出口/没训过/看不到），第二层**深层原因**（generator/verifier 利益冲突），第三层**理论支撑**（验证比生成容易）。第三层是分水岭——能讲清"generator/verifier gap 让独立验证更准"，才算真懂为什么独立 VLM 判断。

---

### 0.5【简历核心】Q13 LoRA 跨本体怎么做的？🧭 ⚠️

👔 面试官：你简历写了 LoRA 跨本体微调，26-DoF 到 15-DoF 维度都变了，怎么处理？

🙋‍♂️ 我：就是加 LoRA 层微调。

👔 面试官：LoRA 是低秩矩阵，维度变了怎么加？边界层怎么处理？

🙋‍♂️ 我：呃……边界层我也加了 LoRA……

👔 面试官：错了。维度变化的边界层不能用 LoRA——形状对不上。边界层必须全量训练，中间语义层才用 LoRA。讲不清这个区分，等于 LoRA 没真用过。

这题是简历核心，**讲不清"边界层全量+语义层 LoRA"的区分，等于跨本体没真做过**。

#### 💡 简要回答

只边界层形状变，中间语义层全复用：ActionEncoder.linear_1 / ActionDecoder 末层 / ProprioEncoder → 全量训练；Action Mixture / Vision Tower / Gemma → LoRA；其余 ~97% 冻结。

#### 📝 详细解析

**跨本体的核心困难：维度变化**

G0Plus 预训练在 R1Lite（26-DoF），我迁移到 Fetch（15-DoF）。动作维度和状态维度都变了——R1Lite 动作 26 维、Fetch 动作 13 维；R1Lite 状态 26 维、Fetch 状态 15 维。维度变了，预训练的边界层（输入输出层）形状对不上，不能直接复用。

**为什么边界层不能用 LoRA？**

LoRA 的数学形式是 `W' = W + BA`，其中 W 是冻结的原权重，B、A 是低秩矩阵。这要求 `W'` 和 `W` **形状相同**——LoRA 是在原权重上加扰动，不是替换。

但边界层形状变了（如 ActionEncoder 从 26 维输入变成 13 维输入），原权重 W 是 26×hidden，新需求是 13×hidden，形状不同，加 LoRA 没意义——根本没有"原权重"可以加扰动。所以边界层必须**全量训练**——重新初始化一个新形状的权重，从零学。

**三层处理策略**

| 层类型 | 处理方式 | 原因 |
|---|---|---|
| 边界层（ActionEncoder.linear_1 / ActionDecoder 末层 / ProprioEncoder） | 全量训练 | 形状变了，无原权重可加 LoRA |
| 语义层（Action Mixture / Vision Tower / Gemma） | LoRA（rank16/alpha32） | 形状不变，原权重可复用，LoRA 微调 |
| 其余 ~97% 参数 | 冻结 | 语义知识通用，不需重训 |

**为什么语义层可以用 LoRA？**

语义层处理的是"图像特征→语义理解""状态特征→动作语义"这类映射，这些映射跨本体通用——SigLIP 编码图像特征的能力不随本体变，Gemma 理解指令的能力不随本体变。所以语义层形状不变，原权重可复用，只需 LoRA 微调让模型适应新本体的分布差异。

**LoRA 参数选择**

rank16/alpha32（缩放=alpha/rank=2），lr 5e-4。rank 越大容量越强但越易过拟合；rank16 是小数据微调的常见配置。alpha 是缩放因子，控制 LoRA 更新的幅度。

**归一化统计量必须重算**

`use_pretrained_norm_stats=False`，从 Fetch 数据重算。因为动作分布不同——R1Lite 和 Fetch 的关节角范围、动作幅度都不同，用 R1Lite 的归一化统计量会让 Fetch 的动作分布失真。

#### 🎤 现场话术（40 秒）

"跨本体的核心困难是维度变化——R1Lite 26-DoF 到 Fetch 15-DoF，动作和状态维度都变了。我的处理是三层：**边界层**（ActionEncoder.linear_1 / ActionDecoder 末层 / ProprioEncoder）形状变了，没有原权重可以加 LoRA，必须**全量训练**重新初始化；**语义层**（Action Mixture / Vision Tower / Gemma）形状不变，原权重可复用，加 **LoRA**（rank16/alpha32，lr 5e-4）微调适应新本体分布；**其余 ~97% 参数冻结**。归一化统计量 use_pretrained_norm_stats=False 从 Fetch 数据重算——动作分布不同必须重估。为什么边界层不能用 LoRA？LoRA 是 W'=W+BA 要求形状相同，边界层形状变了没有原权重可加扰动。"

#### 追问链

- 追"为什么不全量微调？"→ 小数据全量易破坏预训练知识、过拟合；LoRA 参数高效、显存友好（全量 >70G，LoRA 在 40G 可行）。
- 追"rank16/alpha32 为什么？"→ rank16 是小数据微调常见配置；alpha/rank=2 是缩放因子，控制 LoRA 更新幅度。
- 追"vlm_lr_multiplier 0.5 为什么？"→ 给 VLM 部分更小学习率，保护预训练视觉-语言知识，让动作相关层学得更激进些。
- 追"coarse_task / task 双指令？"→ G0 训练吃 `[High]:{coarse_task},[Low]:{task}`。部署时总指令作 coarse_task、当前子任务作 task 透传，对齐训练分布。

#### 考察什么 / 陷阱扣分

**考察什么**：①边界层 vs 语义层的区分 ②为什么边界层不能用 LoRA（形状变） ③LoRA 参数选择（rank/alpha/lr） ④归一化统计量重算

**陷阱/扣分**：❌ 说"全加 LoRA"→ 边界层形状变不能加 ❌ 答不出为什么边界层不能 LoRA → 没理解 LoRA 数学形式 ❌ 用预训练 norm_stats → 动作分布不同必须重算

#### 🎯 面试总结

这题考的是"跨本体怎么处理维度变化"。三层：第一层**问题**（维度变化），第二层**策略**（边界层全量+语义层 LoRA+其余冻结），第三层**原理**（LoRA 要求形状相同所以边界层不能用）。第三层是分水岭——能讲清"LoRA 是 W'=W+BA 要求形状相同，边界层形状变没有原权重"，才算真懂跨本体。

---

### 0.6【实现深挖】Q18 坐标怎么来的？让 VLM 看图算吗？🔥 🧭

👔 面试官：你 VLM 规划出子任务，子任务里有坐标，坐标怎么来的？

🙋‍♂️ 我：让 VLM 看图算坐标。

👔 面试官：VLM 从像素估坐标是最弱的能力，误差 >0.5m 会撞货架。你怎么保证精度？

🙋‍♂️ 我：呃……让 VLM 多看几张图？

👔 面试官：错了。VLM 的空间感知是最弱项，再怎么看图也算不准厘米级坐标。正确做法是**从地图 waypoint 清单"选"坐标**——网格给拓扑，清单给厘米级坐标，VLM 只做语义匹配+路径排序，精度押地图标定不押模型像素感知。讲不清这个，等于导航精度没保障。

这题考的是"精度押在哪"，**讲不清"从清单选不从像素估"，等于导航精度没想透**。

#### 💡 简要回答

不让 VLM 从像素估坐标（最弱、误差 >0.5m 会撞架）。改成从地图 waypoint 清单"选"坐标：网格给拓扑，清单给厘米级坐标。VLM 做语义匹配+路径排序，精度押地图标定不押模型像素感知。

#### 📝 详细解析

**VLM 从像素估坐标为什么不行？**

VLM（如 Qwen2.5-VL）的空间感知是最弱项：
1. **像素到世界坐标的映射需要标定**：VLM 看到的是 2D 图像，要把图像中的位置转成世界坐标，需要相机内外参标定。VLM 没有这个标定信息。
2. **单目深度歧义**：从单张图估距离有尺度歧义——同一个像素位置可能是 1m 外的小物体或 2m 外的大物体。
3. **误差累积**：即使单次估准，多次估的误差会累积，>0.5m 就会撞货架。

**从地图 waypoint 清单"选"坐标**

我的做法是把坐标来源从"VLM 估"改成"地图给"：
- **文本地图**：ASCII 网格 + 图例 + waypoint 清单。网格给拓扑（哪个货架在哪、通道怎么走），waypoint 清单给厘米级坐标（每个关键点的精确 (x,y)）。
- **VLM 的任务**：从 waypoint 清单里"选"——给定指令"去 A 货架拿可乐"，VLM 从清单里找到"A 货架"对应的 waypoint，输出它的坐标。VLM 做的是**语义匹配**（"A 货架"对应清单里哪个条目）+ **路径排序**（先去 A 再去 B），不做**坐标计算**。

**精度押地图标定不押模型像素感知**

这是核心设计思想——把精度押在**可标定、可验证的地图**上，不押在**不可控的模型空间感知**上。地图是预先标定的，坐标精确到厘米；VLM 只负责"选哪个 waypoint"，不负责"算坐标"。这样：
1. 精度有保障：坐标来自标定，不依赖模型估。
2. 可调试：坐标错了查地图标定，不查模型。
3. 可复用：换场景只重标地图，不重训模型。

**VLM 输出 JSON 子任务序列**

VLM 输出是 JSON 子任务序列，每个子任务有 type/instruction/target。`_parse` 校验转 Subtask。target 就是 waypoint 坐标，从清单选的。

#### 🎤 现场话术（40 秒）

"不让 VLM 从像素估坐标——VLM 的空间感知是最弱项，单目深度歧义+误差累积，>0.5m 会撞货架。我的做法是从地图 waypoint 清单'选'坐标：文本地图是 ASCII 网格+图例+waypoint 清单，网格给拓扑、清单给厘米级坐标。VLM 的任务是语义匹配——给定指令'去 A 货架拿可乐'，从清单里找到 A 货架对应的 waypoint 输出它的坐标，加路径排序先去 A 再去 B。VLM 不做坐标计算只做选择。核心思想是**精度押地图标定不押模型像素感知**——地图预先标定精确到厘米，坐标错了查地图不查模型，换场景只重标地图不重训模型。"

#### 追问链

- 追"waypoint 清单怎么来的？"→ 仿真环境预先标定，每个关键点（货架前、通道口、篮子位）的精确 (x,y) 厘米级坐标。
- 追"VLM 选错了怎么办？"→ `_parse` 校验格式错直接失败；语义错（选了 B 货架当 A）靠闭环完成判断兜底——到错了货架 VLM 裁决 FAIL 触发重规划。
- 追"为什么不直接给 VLM 坐标让它规划？"→ VLM 需要语义理解（"可乐"对应哪个货架），纯坐标没有语义信息；清单同时给语义（货架名）和坐标，VLM 做语义到坐标的映射。
- 追"真机部署地图怎么来？"→ 真机要 SLAM 建图+人工标定 waypoint，或用 AR 标记自动定位——这是真机部署的额外工程。

#### 考察什么 / 陷阱扣分

**考察什么**：①VLM 空间感知弱的原因 ②从清单选不从像素估的设计 ③精度押地图不押模型的思想 ④能落回项目（文本地图+waypoint 清单）

**陷阱/扣分**：❌ 说"让 VLM 看图算坐标"→ 致命设计错 ❌ 答不出为什么 VLM 估不准 → 没理解模型能力边界 ❌ 把精度押在模型上 → 工程上不可控

#### 🎯 面试总结

这题考的是"精度押在哪"。三层：第一层**问题**（VLM 空间感知弱），第二层**解法**（从清单选不从像素估），第三层**思想**（精度押地图标定不押模型）。第三层是分水岭——能讲清"把精度押在可标定可验证的地图上，不押在不可控的模型空间感知上"，才算真懂导航精度保障。

---

### 0.7【状态机核心】Q23 何时重规划何时只重试？🔥 🧭

👔 面试官：你 VLM 裁决 FAIL 后，是重试还是重规划？怎么决定？

🙋‍♂️ 我：看任务成没成功。

👔 面试官：FAIL 就是没成功，这我都知道。我问的是"没成功之后"怎么决定——重试同一个子任务，还是重新规划整条路径？

🙋‍♂️ 我：呃……看失败原因？

👔 面试官：不对。决定的不是"成没成功"，是"世界还符不符合剩余计划假设"。抓空但物品还在→RETRY（重规划是浪费）；物品掉地上→REPLAN（前提被破坏，需插 pick_from_floor）。讲不清这个区分，等于 recovery 没设计。

这题考的是"recovery 状态机"，**讲不清"看世界是否破坏剩余计划前提"而非"看成没成功"，等于 recovery 没想透**。

#### 💡 简要回答

决定的不是"成没成功"，是"世界还符不符合剩余计划假设"。抓空但物品还在→RETRY（重规划是浪费）；物品掉地上→REPLAN（前提被破坏，需插 pick_from_floor）。

#### 📝 详细解析

**为什么"看成没成功"是错的？**

FAIL 已经说明没成功了——VLM 裁决 FAIL 就是没成功。问题不是"成没成"，而是"没成之后怎么办"。如果用"成没成功"决定 recovery，那所有 FAIL 都一样处理，没法区分。

**正确的判断维度：世界是否破坏剩余计划前提**

剩余计划是基于"世界状态"规划的——如"去 A 货架拿可乐再放篮子"基于"可乐在 A 货架"这个前提。失败后要看：
- **前提还在吗？** → 如果还在，剩余计划仍有效，只需 RETRY 当前子任务。
- **前提被破坏了吗？** → 如果被破坏，剩余计划失效，必须 REPLAN。

**两个典型场景**

| 场景 | 失败原因 | 世界状态 | 前提 | recovery |
|---|---|---|---|---|
| 抓空 | 夹爪没夹住 | 物品还在货架上 | "可乐在 A 货架"还在 | RETRY（重试 pick_to_basket） |
| 物品掉地 | 夹爪松了物品掉地 | 物品在地上不在货架 | "可乐在 A 货架"被破坏 | REPLAN（插 pick_from_floor） |

**为什么抓空是 RETRY 不是 REPLAN？**

抓空时世界没变——物品还在货架上，剩余计划"去 A 货架拿可乐再放篮子"仍有效。重试 pick_to_basket 就行，重新规划整条路径是浪费（路径没变、目标没变）。

**为什么物品掉地是 REPLAN 不是 RETRY？**

物品掉地时世界变了——物品从货架到了地上，剩余计划"去 A 货架拿可乐"的前提"可乐在 A 货架"被破坏。重试 pick_to_basket 没用（货架上没东西可抓），必须重新规划——插入一个 pick_from_floor 子任务把地上的物品捡起来。

**verifier 职责单一**

verifier（VLM 裁决）只回答"完成没有"（IN_PROGRESS/SUCCESS/FAIL），不决定 recovery 策略。recovery 策略由**控制器层**根据"世界是否破坏剩余计划前提"决定。这样 verifier 简单（只判三态），控制器层复杂（决定 RETRY/REPLAN），职责分离。

**我的局限：全量重排非增量**

我现在的 REPLAN 是全量重排——重新规划整条路径，不是只插入一个 pick_from_floor。这会导致路径抖动（前面已经走过的路被重新规划成不同路径）。更高级的做法是增量重规划——只插入缺失的子任务，保留前面已执行的部分。这是我主动暴露的局限。

#### 🎤 现场话术（40 秒）

"决定的不是'成没成功'——FAIL 已经说明没成功。问题是'没成之后怎么办'，判断维度是'世界还符不符合剩余计划假设'。剩余计划基于世界状态规划，如'去 A 货架拿可乐'基于'可乐在 A 货架'。失败后看前提：抓空但物品还在货架上→前提还在→RETRY 重试 pick_to_basket，重规划是浪费；物品掉地上→'可乐在 A 货架'被破坏→REPLAN 插入 pick_from_floor。verifier 职责单一只判三态 IN_PROGRESS/SUCCESS/FAIL，recovery 策略由控制器层根据世界是否破坏前提决定。局限：我现在 REPLAN 是全量重排会路径抖动，更高级是增量重规划只插缺失子任务。"

#### 追问链

- 追"怎么知道物品掉地上了？"→ VLM 裁决 FAIL 时看头部前后帧，能判断物品位置变化（在货架 vs 在地上）。
- 追"REPLAN 全量重排有什么问题？"→ 路径抖动——前面已走的路被重新规划成不同路径，效率低且可能撞已避开的障碍。
- 追"增量重规划怎么做？"→ 保留已执行子任务，只在失败点插入新子任务（如 pick_from_floor），后续子任务不变。
- 追"重试次数上限？"→ 有，避免死循环——同一子任务重试 N 次仍 FAIL 就升级 REPLAN 或报错求助。

#### 考察什么 / 陷阱扣分

**考察什么**：①判断维度（世界是否破坏前提，不是成没成功） ②RETRY vs REPLAN 的区分 ③verifier 职责单一 ④诚实暴露局限（全量重排）

**陷阱/扣分**：❌ 说"看成没成功"→ FAIL 已经说明没成功 ❌ 把 recovery 塞进 verifier → 职责不清 ❌ 不暴露全量重排局限 → 不诚实

#### 🎯 面试总结

这题考的是"recovery 状态机"。三层：第一层**问题**（FAIL 后怎么办），第二层**判断维度**（世界是否破坏剩余计划前提），第三层**职责分离**（verifier 只判三态，控制器层决定 RETRY/REPLAN）。第三层是分水岭——能讲清"verifier 职责单一，recovery 由控制器层根据世界是否破坏前提决定"，才算真懂状态机设计。

---

### 0.8【评估核心】Q31 靠什么判好坏？为什么必须闭环评估？🔥 🧭

👔 面试官：你怎么判断模型训得好不好？

🙋‍♂️ 我：看 validation loss。

👔 面试官：val loss 低就代表抓得起物品吗？

🙋‍♂️ 我：呃……应该差不多吧，loss 低说明动作预测准……

👔 面试官：错了。模仿学习的 val loss 和成功率相关很弱——动作 MSE 会累积误差，loss 低≠抓得起。唯一可信的是闭环评估：放进仿真真跑数成功率。讲不清这个，等于评估没做对。

这题考的是"评估方法"，**讲不清"val loss≠成功率，必须闭环评估"，等于评估没想透**。

#### 💡 简要回答

开环比动作 MSE 会累积误差，val loss 低≠抓得起。闭环=放进仿真真跑数成功率，唯一可信。闭环不预生成轨迹，只生成 test 场景现场 rollout，跑 20~30 episode，必须用 test 场景（未见布局）否则虚高。

#### 📝 详细解析

**为什么 val loss 不可信？**

模仿学习（BC）的 val loss 通常是动作 MSE——预测动作和专家动作的均方误差。但这个指标和任务成功率相关很弱，原因：
1. **误差累积**：单步动作预测准（loss 低），但执行时小误差累积，几步后状态偏离专家轨迹，后续动作全错。开环动作 MSE 看不到这个累积。
2. **多模态动作分布**：动作分布是多模态的（同一状态多种合理动作），MSE 会取平均落到不可行的中间值。loss 低可能是"取了平均"，但取平均的动作不可行。
3. **任务级 vs 步级**：loss 是步级指标（每步动作准不准），任务是任务级指标（整条轨迹跑完任务成不成）。步级准不代表任务级成。

**闭环评估是什么？**

闭环评估=把模型放进仿真环境，让它自己跑（rollout），数成功率：
1. **不预生成轨迹**：不是拿专家轨迹比动作，是模型自己看图像出动作，环境执行动作后出新图像，模型再看新图像出下一步——闭环。
2. **现场 rollout**：每个 episode 从初始状态开始，模型自己跑到结束（成功/失败/超时）。
3. **数成功率**：成功 episode 数 / 总 episode 数。

**为什么必须用 test 场景？**

test 场景是训练时没见过的布局（不同货架位置、不同物品摆放）。如果用训练场景评估，模型可能只是"背"了训练数据，成功率虚高。用 test 场景才测泛化——模型在新布局能不能成。

**跑多少 episode？**

20~30 episode。少了统计不显著（20% 成功率差可能是随机波动），多了耗时（每个 episode 要跑几十秒）。20~30 是统计显著性和时间的平衡。

**闭环完成判断 vs 闭环评估（易混）**

这两个不是一回事：
- **闭环完成判断**=部署时实时切子任务（运行时）——传感器触发+VLM 裁决，判断当前子任务完成没。
- **闭环评估**=离线数成功率（开发时）——跑完所有 episode 数成功率。

一个在运行时（实时切子任务），一个在开发时（离线评估），不要混。

#### 🎤 现场话术（40 秒）

"val loss 不可信——模仿学习的 val loss 是动作 MSE，但误差会累积：单步准不代表整条轨迹准，几步后状态偏离专家轨迹后续全错。而且动作分布多模态，MSE 取平均可能落到不可行中间值，loss 低不代表动作可行。唯一可信是闭环评估：把模型放进仿真自己 rollout，数成功率。不预生成轨迹比动作，是模型自己看图像出动作、环境执行后出新图像、模型再看新图像出下一步——闭环。跑 20~30 episode，必须用 test 场景（训练时没见过的布局）否则模型只是背训练数据成功率虚高。闭环完成判断和闭环评估不是一回事——前者是运行时实时切子任务，后者是开发时离线数成功率。"

#### 追问链

- 追"val loss 完全没用吗？"→ 当训练哨兵有用——loss 不降说明训练没动；但选模型不能靠它，靠闭环成功率。
- 追"闭环评估为什么不用训练场景？"→ 训练场景可能"背"了，成功率虚高；test 场景才测泛化。
- 追"20~30 episode 够吗？"→ 统计显著性和时间的平衡；少了不显著，多了耗时。正式 benchmark 会跑更多。
- 追"闭环完成判断和闭环评估区别？"→ 完成判断是运行时实时切子任务（传感器+VLM 裁决）；评估是开发时离线数成功率（rollout 数成功 episode 比例）。

#### 考察什么 / 陷阱扣分

**考察什么**：①val loss 为什么不可信（误差累积+多模态平均） ②闭环评估的定义（rollout 数成功率） ③必须用 test 场景（测泛化） ④闭环完成判断 vs 闭环评估的区分

**陷阱/扣分**：❌ 说"看 val loss 选模型"→ 致命错 ❌ 用训练场景评估 → 虚高 ❌ 把闭环完成判断和闭环评估混 → 概念错

#### 🎯 面试总结

这题考的是"评估方法"。三层：第一层**问题**（val loss 不可信），第二层**解法**（闭环 rollout 数成功率），第三层**细节**（test 场景测泛化+20~30 episode+完成判断 vs 评估区分）。第三层是分水岭——能讲清"闭环评估用 test 场景 rollout 数成功率，和运行时完成判断不是一回事"，才算真懂评估。

---

### 0.9【原理核心】Q46 flow matching 和 diffusion 的关系与区别？🔥

👔 面试官：你项目用 flow matching 生成动作，它和 diffusion 什么关系？为什么不用 diffusion？

🙋‍♂️ 我：都是从噪声生成目标，flow matching 更快。

👔 面试官：快在哪？原理上有什么根本区别？

🙋‍♂️ 我：呃……flow matching 步数少……

👔 面试官：步数少是表象。根本区别是——diffusion 学逐步去噪的得分，flow matching 学一条从噪声到数据的概率流 ODE 的速度场。flow 路径更直、推理积分步数更少。讲不清这个，等于不懂为什么用 flow。

这题考的是"动作生成原理"，**讲不清"学速度场 vs 学得分"的根本区别，等于 flow matching 没懂**。

#### 💡 简要回答

都从噪声生成目标。Diffusion 学逐步去噪的得分；flow matching 学一条从噪声到数据的概率流 ODE 的速度场，训练目标是回归速度向量。flow 路径更直、推理积分步数更少、更稳。

#### 📝 详细解析

**Diffusion 的原理**

Diffusion 模型（DDPM/DDIM）的思路：
1. **前向过程**：逐步给数据加噪声，从 x0（数据）到 xT（纯噪声），这是一个马尔可夫链。
2. **反向过程**：学一个网络预测每一步的"去噪方向"，从 xT 逐步去噪回 x0。
3. **训练目标**：学的是**得分函数**（score function）——噪声分布的梯度，告诉网络"往哪个方向去噪"。
4. **推理**：从纯噪声出发，按学到的得分逐步去噪，需要很多步（如 1000 步）。

**Flow matching 的原理**

Flow matching（如 pi0/G0 用的）的思路：
1. **概率流 ODE**：定义一条从噪声分布到数据分布的连续路径（不是离散马尔可夫链），用 ODE 描述：dx/dt = v(x,t)，其中 v 是速度场。
2. **训练目标**：学这个**速度场 v**——给定当前位置 x 和时间 t，预测"应该往哪个方向走多快"。训练目标是回归速度向量（不是得分）。
3. **推理**：从纯噪声出发，沿学到的速度场积分（如欧拉法）到 t=1 得到数据。步数可以很少（如 10 步）。

**根本区别：学什么**

| | Diffusion | Flow matching |
|---|---|---|
| 学什么 | 得分函数（噪声梯度） | 速度场（ODE 速度向量） |
| 路径 | 弯曲（马尔可夫链） | 直（概率流 ODE） |
| 推理步数 | 多（100~1000 步） | 少（10 步） |
| 训练目标 | 预测噪声 ε | 回归速度向量 v |

**为什么 flow 路径更直？**

Diffusion 的前向是逐步加噪的马尔可夫链，反向去噪路径是弯曲的——每一步去噪方向都不同，要很多步才能从噪声走到数据。

Flow matching 定义的是一条**直线路径**——从噪声 x0 到数据 x1 的线性插值 `ψ_t = t·x1 + (1-t)·x0`，速度场是常数（指向 x1-x0 方向）。路径直意味着积分步数可以少——10 步欧拉积分就能从噪声走到数据。

**为什么动作生成适合用 flow/diffusion 而非回归？**

动作分布是多模态的——同一状态多种合理动作（如抓杯子可以从左边抓也可以从右边抓）。回归（MSE）会取平均落到不可行的中间值（两边都不靠）。生成式建模（flow/diffusion）建模整个分布，能采样出某一个合理模态。

**G0 里 flow matching 的具体实现**

- `x0` = 噪声，`x1` = 动作，`ψ_t` 是 t 时刻插值 `τ·x1 + (1-τ)·x0`。
- 模型预测从插值点指向数据的速度场。
- flow matching loss 监督这个速度场预测。
- 推理时从随机噪声出发，沿速度场积分 10 步得到动作块。
- 时间步 t 通过 time_embedding 注入网络（告诉网络当前在积分的哪一步）。

#### 🎤 现场话术（40 秒）

"都从噪声生成目标，但学的东西不同。Diffusion 学逐步去噪的得分函数——噪声分布的梯度，前向是马尔可夫链加噪、反向逐步去噪，路径弯曲、推理要 100~1000 步。Flow matching 学一条从噪声到数据的概率流 ODE 的速度场——给定位置和时间预测往哪走多快，训练目标是回归速度向量。flow 路径是直线（线性插值 ψ_t = t·x1+(1-t)·x0），积分步数少，G0 只用 10 步。根本区别是学什么：diffusion 学得分，flow 学速度场。动作生成适合用 flow 不用回归，因为动作分布多模态——同一状态多种合理动作，回归 MSE 取平均落到不可行中间值，flow 建模整个分布能采样出合理模态。G0 里 x0 是噪声 x1 是动作，模型预测速度场，10 步欧拉积分出动作块。"

#### 追问链

- 追"为什么 flow 路径直？"→ 定义的是线性插值 ψ_t = t·x1+(1-t)·x0，速度场是常数指向 x1-x0 方向，路径是直线。
- 追"10 步够吗？"→ 够，因为路径直，欧拉积分 10 步就收敛；diffusion 路径弯要更多步。
- 追"动作为什么多模态？"→ 同一状态多种合理动作（抓杯子可左抓可右抓），回归取平均两边不靠。
- 追"time_embedding 怎么注入？"→ 时间步 t 编码成向量，告诉网络当前在积分的哪一步；adaptive 模式用 AdaLN 调制，否则拼接到输入。

#### 考察什么 / 陷阱扣分

**考察什么**：①学什么不同（得分 vs 速度场） ②路径不同（弯曲 vs 直线） ③步数不同（多 vs 少） ④动作为什么适合生成式（多模态）

**陷阱/扣分**：❌ 只说"flow 更快"不讲原理 → 没懂 ❌ 把 flow 说成"改进的 diffusion" → 是不同的数学框架 ❌ 答不出动作为什么多模态 → 没理解生成式建模动机

#### 🎯 面试总结

这题考的是"动作生成原理"。三层：第一层**关系**（都从噪声生成），第二层**区别**（学得分 vs 学速度场，路径弯 vs 直），第三层**动机**（动作多模态所以用生成式）。第三层是分水岭——能讲清"动作分布多模态，回归取平均不可行，所以用 flow 建模整个分布"，才算真懂为什么用 flow matching。

---

### 0.10【方法对比】Q55 这是模仿学习还是 RL？区别？🔥 🧭

👔 面试官：你项目是模仿学习还是强化学习？

🙋‍♂️ 我：是模仿学习。

👔 面试官：BC 和 RL 本质区别是什么？BC 有什么硬伤？

🙋‍♂️ 我：BC 从演示学，RL 试错。BC 的硬伤是……数据少？

👔 面试官：数据少不是硬伤。BC 的硬伤是**分布漂移**——测试时策略自己的动作把状态带到训练没见过的分布，误差累积。讲不清这个，等于不懂 BC 的局限。

这题考的是"方法本质"，**讲不清"分布漂移"和"BC vs RL 的根本区别"，等于不懂自己用的方法**。

#### 💡 简要回答

模仿学习（behavior cloning）——从专家演示监督学动作，无奖励、无环境交互。RL 靠奖励试错。BC 简单稳定但有分布漂移问题——测试时策略自己的动作把状态带到训练没见过的分布，误差累积。

#### 📝 详细解析

**BC 和 RL 的根本区别**

| | BC（行为克隆） | RL（强化学习） |
|---|---|---|
| 学习信号 | 专家演示（状态-动作对） | 奖励（环境反馈） |
| 是否交互 | 否（离线学演示） | 是（在线试错） |
| 目标 | 模仿专家 | 最大化累积奖励 |
| 数据需求 | 专家演示 | 环境交互+奖励设计 |
| 安全性 | 高（不试错） | 低（真机试错危险） |

BC 本质是监督学习——输入状态，输出动作，标签是专家动作。RL 是试错——策略出动作，环境给奖励，策略根据奖励调整。

**BC 的硬伤：分布漂移（covariate shift）**

BC 训练时，状态来自专家轨迹（专家访问的状态分布）。测试时，策略自己的动作可能和专家不同，导致状态偏离专家轨迹——状态进入训练时没见过的分布。在没见过的分布上，策略不知道怎么出动作，误差进一步累积，状态进一步偏离——**雪球效应**。

这就是**复合误差（compounding error）**：单步小误差累积成大误差，几步后状态完全偏离专家轨迹。

**为什么 RL 没有这个问题？**

RL 在训练时就和环境交互——策略访问到的状态分布就是它测试时会访问的。所以 RL 没有"训练分布 vs 测试分布"的漂移问题。但 RL 有自己的困难：奖励设计难、样本效率低、真机试错不安全。

**我怎么缓解分布漂移？**

用 receding horizon（滑窗）：VLA 出 16/32 步 action chunk，只执行前 5 步（replan_steps=5），然后用最新观测重新推理。这样：
1. 每 5 步用最新观测纠偏，不让误差累积。
2. action chunk 一次预测多步，减少决策频率、动作更平滑。

**DAgger（交互式补数据）**

更彻底的解法是 DAgger——执行时让专家在策略访问到的新状态补标注，主动把测试分布拉回训练分布。我没用但知道原理。

**为什么我选 BC 不选 RL？**

1. 真机 RL 试错不安全——操作任务试错可能损坏物品或机器人。
2. 有现成演示数据（运动规划生成），BC 性价比高。
3. VLA 主流（RT/pi0/G0）都是大规模 BC 预训练。

**BC 能超越演示者吗？**

不能，纯 BC 上限就是模仿演示者。想超越要加 RL——先 BC 预训练拿先验，再 RL 精调挤上限。这是当前 VLA+RL 的前沿方向。

#### 🎤 现场话术（40 秒）

"是模仿学习 BC——从专家演示监督学状态到动作映射，无奖励、不试错。RL 是靠奖励试错优化策略。BC 的硬伤是分布漂移——训练时状态来自专家轨迹，测试时策略自己的动作把状态带到训练没见过的分布，误差累积成雪球。我用 receding horizon 缓解：出 16 步 action chunk 只执行前 5 步，用最新观测重新推理纠偏，不让误差累积。选 BC 不选 RL 三原因：真机 RL 试错不安全；有现成演示数据 BC 性价比高；VLA 主流都是大规模 BC 预训练。BC 上限就是模仿演示者，想超越要加 RL——先 BC 预训练再 RL 精调是前沿。"

#### 追问链

- 追"分布漂移和 shortcut learning 什么关系？"→ 分布漂移是 BC 通病（轨迹偏离训练分布）；shortcut learning 是另一类（学错因果）。我项目主要是 shortcut learning（数据有捷径），分布漂移靠 receding horizon 缓解。
- 追"DAgger 你了解吗？"→ 了解：执行时让专家在新状态补标注，主动把测试分布拉回训练分布；我没用但知道原理。
- 追"BC 能超越演示者吗？"→ 不能，纯 BC 上限就是模仿演示者；想超越要加 RL。
- 追"为什么不用 RL？"→ 真机 RL 试错不安全、样本效率低、reward 难设计；有演示数据 BC 性价比高。

#### 考察什么 / 陷阱扣分

**考察什么**：①BC/RL 本质区别（监督 vs 试错） ②BC 的硬伤（分布漂移+复合误差） ③解法（receding horizon） ④为什么选 BC

**陷阱/扣分**：❌ 只说"BC 简单"不提漂移 → 没抓硬伤 ❌ 把 RL 一棍子打死 → 前沿是 BC+RL ❌ 答不出分布漂移是什么 → 不懂 BC 局限

#### 🎯 面试总结

这题考的是"方法本质"。三层：第一层**区别**（BC 监督学 vs RL 试错），第二层**BC 硬伤**（分布漂移+复合误差），第三层**解法**（receding horizon 纠偏）。第三层是分水岭——能讲清"分布漂移是测试状态偏离训练分布，receding horizon 每 5 步用最新观测纠偏"，才算真懂 BC 的局限和解法。

---

### 0.11【领域横向】Q59 知道哪些 VLA 工作？G0 和它们什么关系？🔥 🧭

👔 面试官：你知道哪些 VLA 工作？G0 和它们什么关系？

🙋‍♂️ 我：知道 RT-1、RT-2、OpenVLA、pi0，G0 就是类似 pi0。

👔 面试官：类似在哪？RT-2 和 pi0 根本路线不同你知道吗？

🙋‍♂️ 我：呃……都是 VLA……

👔 面试官：讲不清路线分歧，等于不了解领域。RT-2 是动作当文本 token 自回归，pi0 是流匹配连续动作——根本分歧在动作表示。G0 是 pi0 路线+双系统。讲不清这个，等于领域横向没做。

这题考的是"领域横向"，**讲不清"动作表示的根本分歧"和"G0 在领域里的位置"，等于不了解 VLA 领域**。

#### 💡 简要回答

- **RT-1/RT-2**（Google）：token 化动作，RT-2 把动作当文本 token 输出首次展示语义泛化。
- **OpenVLA**（2024）：7B 开源，Llama2+DINOv2/SigLIP，自回归离散动作。
- **pi0/pi0-FAST**（2024, PI）：PaliGemma+flow matching action expert+action chunking。**G0 的直接技术参照**。
- **Octo**（2024）：小型、扩散动作头。
- **GR00T N1**（2025, NVIDIA）/ **Helix**（2025, Figure）：双系统人形。
- **G0/G0Plus**：pi0 式流匹配执行器+双系统规划+大规模开放世界数据集。

#### 📝 详细解析

**VLA 的发展脉络**

VLA（Vision-Language-Action）模型的发展可以分几个阶段：

**阶段 1：早期 token 化动作（2022-2023）**
- **RT-1**（2022, Google）：Transformer+离散化动作 token，大规模真机 BC 的开山。动作离散成 bin，当 token 输出。
- **RT-2**（2023）：把 VLM（PaLI-X）直接当策略，动作当文本 token 输出，首次展示**语义泛化**——能听懂没见过的指令（"把可乐放到星巴克杯子上"）。

**阶段 2：开源基线+小型化（2024）**
- **OpenVLA**（2024）：7B 开源，Llama2+DINOv2/SigLIP，自回归离散动作。开源基线，但动作离散有量化损失。
- **Octo**（2024）：小型、扩散动作头、可灵活换观测/动作空间。

**阶段 3：流匹配+双系统（2024-2025）**
- **pi0/pi0-FAST**（2024, Physical Intelligence）：PaliGemma backbone+**flow matching action expert**+action chunking；FAST 是动作的频域离散 tokenizer。**G0/G0Plus 的直接技术参照**。
- **GR00T N1**（2025, NVIDIA）：双系统，慢推理+扩散动作，人形。
- **Helix**（2025, Figure）：双系统人形，高频全身控制。
- **G0/G0Plus**（本项目）：pi0 式流匹配执行器+双系统规划+Galaxea 开放世界数据集。

**根本分歧：动作表示**

VLA 的根本分歧在**动作表示**：

| 路线 | 代表 | 动作表示 | 优点 | 缺点 |
|---|---|---|---|---|
| 离散 token | RT-2、OpenVLA | 动作离散成 token，自回归交叉熵 | 复用 LM 训练范式、易和语言统一 | 量化损失、长 chunk token 多、采样慢 |
| 连续流匹配 | pi0、G0 | 独立 action expert 流匹配生成 | 高精度、多模态、一次出整块 | 要额外 action expert、架构复杂 |

**G0 的位置：pi0 路线+双系统**

G0 在 pi0 基础上加了：
1. **双系统规划**：System2 VLM 拆子任务（对标 Helix/GR00T 的快慢分层）。
2. **大规模开放世界数据**：Galaxea 开放世界数据集（500h+真实移动操作）。
3. **跨本体设计**：LoRA 微调框架和 R1Lite→Fetch 跨本体接口。

G0 的核心相似点是 pi0：VLM backbone+flow matching action expert+action chunking+共享注意力/KV cache。G0 在其上加了双系统规划和大规模数据。

**为什么我基于 G0 不基于 OpenVLA？**

G0 是**双系统+大规模真实移动操作数据+跨本体设计**，正好对上我的仓储补货长程任务；OpenVLA 偏单体桌面操作，没有双系统规划和移动操作数据。且 G0Plus 提供 LoRA 微调框架和跨本体接口，迁移成本低。

#### 🎤 现场话术（40 秒）

"VLA 发展几阶段：早期 RT-1/RT-2 是 token 化动作——RT-2 把动作当文本 token 输出首次展示语义泛化。2024 年 OpenVLA 开源基线、Octo 小型扩散头。2024-2025 年 pi0 用 PaliGemma+flow matching action expert+action chunking，是 G0 的直接技术参照。GR00T N1、Helix 是双系统人形。G0 在 pi0 基础上加了双系统规划+大规模开放世界数据+跨本体设计。根本分歧在动作表示：RT-2/OpenVLA 是离散 token 自回归，复用 LM 范式但有量化损失；pi0/G0 是连续流匹配，高精度多模态但要额外 action expert。G0 的核心相似点是 pi0——VLM backbone+flow matching+共享注意力+KV cache 复用。我基于 G0 是因为它有双系统+移动操作数据+跨本体接口，对上仓储补货长程任务。"

#### 追问链

- 追"RT-2 把动作当文本 token，具体怎么做？"→ 动作离散成 bin，每个 bin 当一个 token，和文本 token 一起自回归输出，用交叉熵训练。
- 追"pi0 和 G0 的核心相似点？"→ 都是 VLM backbone+flow matching action expert+action chunking+共享注意力/KV cache。G0 在其上加了双系统规划和大规模数据。
- 追"OpenVLA 为什么用 SigLIP+DINOv2？"→ SigLIP 给语义对齐（理解指令），DINOv2 给空间结构（精确定位），机器人操作两者都要；G0 只用 SigLIP，空间靠 flow matching 从数据学。
- 追"π0.5 用 RL 了吗？"→ **没有！** π0.5 泛化靠 co-training（97.6% 数据来自其他机器人/web）+分层推理+知识隔离（stop-gradient），不是 RL。别张冠李戴。

#### 考察什么 / 陷阱扣分

**考察什么**：①VLA 发展脉络（RT-1→RT-2→OpenVLA→pi0→G0） ②动作表示的根本分歧（离散 token vs 连续流匹配） ③G0 在领域里的位置（pi0 路线+双系统） ④为什么基于 G0

**陷阱/扣分**：❌ 把 G0 说成"和 pi0 差不多"→ G0 加了双系统+数据+跨本体 ❌ 把 π0.5 说成用 RL → 致命错误 ❌ 答不出动作表示分歧 → 不了解领域

#### 🎯 面试总结

这题考的是"领域横向"。三层：第一层**发展脉络**（RT-1→RT-2→OpenVLA→pi0→G0），第二层**根本分歧**（离散 token vs 连续流匹配），第三层**G0 的位置**（pi0 路线+双系统+数据+跨本体）。第三层是分水岭——能讲清"G0 在 pi0 基础上加了双系统规划和大规模数据"，才算真懂 G0 在领域里的定位。

---

### 0.12【原理深挖】Q63 LoRA 原理？为什么有效？🔥

👔 面试官：LoRA 原理讲讲？为什么有效？

🙋‍♂️ 我：就是冻结原权重，加一个低秩矩阵。

👔 面试官：为什么低秩矩阵够用？理论基础是什么？

🙋‍♂️ 我：呃……因为参数少……

👔 面试官：参数少是结果不是原因。理论基础是"微调的权重更新本身是低秩的"——所以少量参数够用。讲不清这个，等于不懂 LoRA 为什么 work。

这题考的是"LoRA 原理"，**讲不清"权重更新低秩"的理论基础，等于 LoRA 没懂**。

#### 💡 简要回答

冻结原权重 W，加低秩旁路 W'=W+BA（B、A 低秩）。假设微调的权重更新本身是低秩的，所以少量参数够用。推理可合并回 W 无额外延迟。

#### 📝 详细解析

**LoRA 的数学形式**

LoRA（Low-Rank Adaptation）的数学形式：
- 原权重 W（d×d），冻结不动。
- 加低秩旁路：W' = W + B·A，其中 B（d×r）、A（r×d），r << d（如 r=16, d=2048）。
- 训练时只更新 B 和 A，W 不动。
- 推理时可合并：W' = W + BA，合并后和原权重形状相同，无额外延迟。

**为什么低秩够用？理论基础**

LoRA 的核心假设：**微调时权重的更新 ΔW 本身是低秩的**。

这个假设来自经验观察：预训练大模型在下游任务微调时，权重的"变化量"ΔW 的有效秩远小于 d。也就是说，虽然 ΔW 是 d×d 矩阵，但它的"信息"集中在少数几个方向上——可以用 r 维（r << d）的低秩矩阵近似。

理论支撑来自 Aghajanyan 等（2020）的研究：预训练模型在微调时，任务的"内在维度"（intrinsic dimension）远小于模型参数维度。所以用低秩矩阵 BA 近似 ΔW 是够的。

**为什么推理无延迟？**

训练时 W' = W + BA，要额外算 BA。但推理时可以**合并**：W_merged = W + BA，合并后和原权重形状相同，推理时只算 W_merged·x，和原模型完全一样，无额外延迟。

这是 LoRA 相比 adapter/prefix tuning 的核心优势——adapter 加层有推理延迟，prefix tuning 改输入容量更小，LoRA 合并后零延迟。

**rank 和 alpha 怎么选？**

- **rank r**：r 越大容量越强（能近似更复杂的 ΔW），但参数越多越易过拟合/越费显存。r=16 是小数据微调的常见配置；r=64/128 用于更复杂任务。
- **alpha α**：是缩放因子，有效缩放是 α/r。α=32, r=16 时缩放=2，控制 LoRA 更新的幅度。α 大更新幅度大，α 小更新幅度小。

**哪些层加 LoRA？**

通常加在注意力的 q/k/v/o 投影（Transformer 的核心）。本项目还覆盖 Vision Tower 和 Action Mixture。维度变化的边界层不能用 LoRA（形状对不上），必须全量训练（见 0.5 节）。

**LoRA vs 其他 PEFT 方法**

| 方法 | 怎么改 | 推理延迟 | 容量 |
|---|---|---|---|
| LoRA | 加低秩旁路 BA，可合并回 W | 无 | 中 |
| Adapter | 加额外层 | 有 | 中 |
| Prefix/Prompt tuning | 改输入不改权重 | 无 | 小 |
| 全量微调 | 改所有参数 | 无 | 大 |

LoRA 是当前主流——无推理延迟、容量适中、可插拔（不同任务用不同 BA，切换时换 BA 不换 W）。

#### 🎤 现场话术（40 秒）

"LoRA 数学形式是 W'=W+BA，原权重 W 冻结，加低秩旁路 B·A，B 是 d×r、A 是 r×d，r 远小于 d 如 r=16。核心假设是微调时权重的更新 ΔW 本身是低秩的——预训练大模型在下游任务微调时，任务的内在维度远小于模型参数维度，所以用低秩矩阵 BA 近似 ΔW 够用。推理时可合并 W_merged=W+BA，合并后和原权重形状相同无额外延迟——这是 LoRA 相比 adapter 的核心优势，adapter 加层有延迟。rank r 越大容量越强但越易过拟合，r=16 是小数据微调常见配置；alpha 是缩放因子，有效缩放 alpha/r=2 控制更新幅度。通常加在注意力 q/k/v/o 投影，本项目还覆盖 Vision Tower 和 Action Mixture，但维度变化的边界层不能用 LoRA 必须全量训练。"

#### 追问链

- 追"为什么微调的权重更新是低秩的？"→ 预训练模型已有丰富表征，下游任务只需在少数方向上调整——任务的内在维度远小于模型参数维度。
- 追"rank 越大越好吗？"→ 不一定，r 大容量强但易过拟合，小数据 r=16 够用；r 大用于更复杂任务。
- 追"LoRA 能合并，那训练时为什么不直接合并？"→ 训练时要分别更新 B 和 A（低秩约束），合并后就是全秩矩阵失去低秩约束；推理时才合并。
- 追"LoRA 和全量微调效果差多少？"→ 小数据下 LoRA 往往更好（限制可训参数抗过拟合）；大数据全量微调上限更高。

#### 考察什么 / 陷阱扣分

**考察什么**：①LoRA 数学形式（W'=W+BA） ②理论基础（权重更新低秩） ③推理无延迟（可合并） ④rank/alpha 选择 ⑤vs 其他 PEFT

**陷阱/扣分**：❌ 只说"参数少"不讲理论基础 → 没懂 ❌ 说"LoRA 有推理延迟"→ 可合并无延迟 ❌ 把 LoRA 说成"加层"→ 是加旁路不是加层

#### 🎯 面试总结

这题考的是"LoRA 原理"。三层：第一层**数学形式**（W'=W+BA），第二层**理论基础**（权重更新低秩），第三层**优势**（推理无延迟+可插拔）。第三层是分水岭——能讲清"LoRA 可合并回 W 无延迟，相比 adapter 是核心优势"，才算真懂为什么 LoRA 是主流。

---

### 0.13【诚实划界】Q79 项目跑通了吗？成功率多少？🔥 ⚠️ 🧭

👔 面试官：你这个项目跑通了吗？成功率多少？

🙋‍♂️ 我：跑通了，成功率……大概 70% 吧。

👔 面试官：70% 是怎么测的？多少 episode？test 场景还是训练场景？

🙋‍♂️ 我：呃……具体数字我不太记得了……

👔 面试官：编数字最危险。没跑就说没跑，在做就说在做。编了追问必翻车——多少 episode、test 还是 train、怎么定义成功，一个答不上就露馅。

这题是诚实题，**编数字 = 自杀**。没跑就说没跑，在做就说在做，诚实是第一位。

#### 💡 简要回答

如实说：架构链路端到端跑通（编译+sim 验证），跨本体微调配置就绪；闭环成功率还在评估中/待补。不编数字——编了追问必翻车。

#### 📝 详细解析

**为什么不能编数字？**

面试官问成功率，不是要一个数字，是要验证你真做过评估。编一个"70%"，面试官会追：
- "多少 episode 测的？"——20 还是 200？
- "test 场景还是训练场景？"——训练场景虚高。
- "怎么定义成功？"——抓起来算成功，还是放到篮子算成功？
- "失败模式是什么？"——抓空、掉地、撞架？
- "和 baseline 比吗？"——和谁比，差多少？

一个答不上就露馅——编数字的人不会想到这些细节。所以**编数字 = 自杀**。

**诚实怎么说？**

分层诚实：
1. **架构链路端到端跑通**：编译通过、sim 里能跑起来、各模块（VLM 规划、VLA 执行、闭环判断）能协作。这是事实，可以说。
2. **跨本体微调配置就绪**：LoRA 配置、数据、训练脚本都准备好。这也是事实。
3. **闭环成功率还在评估中**：没跑完闭环评估，或刚开始跑。这是诚实划界。
4. **主动说局限**：数据量小（240 条）、judge 是通用 API 未微调、重规划是全量重排——主动暴露局限比被问出来好。

**为什么主动暴露局限？**

面试官追局限时，主动说比被动承认好：
- 主动说 = "我清楚自己的边界，是专业的"。
- 被问出来 = "你没想到这些，是不专业的"。

所以主动说"数据量小定位是可行性验证不是刷 SOTA"、"judge 是通用 API 未微调"、"重规划是全量重排非增量"——这些局限主动暴露，反而加分。

**如果真有成功率数字怎么说？**

如果真跑了闭环评估，要能答：
- 多少 episode（20~30）。
- test 场景还是训练场景（必须 test）。
- 怎么定义成功（任务级：物品放到篮子）。
- 失败模式（抓空、掉地、撞架）。
- 和 baseline 比（和谁比，差多少）。

答得出这些细节，数字才可信。

#### 🎤 现场话术（30 秒，诚实划界）

"⚠️ 诚实讲：架构链路端到端跑通——编译通过、sim 里能跑起来、VLM 规划、VLA 执行、闭环判断各模块能协作。跨本体微调配置就绪——LoRA 配置、数据、训练脚本都准备好。但闭环成功率还在评估中，我还没跑完完整的 test 场景 rollout。我不编数字——编了追问多少 episode、test 还是 train、怎么定义成功，一个答不上就露馅。局限我主动说：数据量小 240 条定位是可行性验证不是刷 SOTA；judge 是通用 API 未微调；重规划是全量重排非增量。这些都是我清楚自己的边界。"

#### 追问链

- 追"为什么没跑完闭环评估？"→ 时间和算力限制；闭环评估要跑 20~30 episode 每个 episode 几十秒，还要调 test 场景。
- 追"你定位是什么？"→ 参数高效跨本体迁移的可行性验证——证明 G0Plus 能通过 LoRA 迁到 Fetch 且链路跑通，不是刷 SOTA 成功率。
- 追"如果给你更多资源下一步做什么？"→ ①跑完整闭环评估报 train/test/OOD 成功率；②机器人数据 SFT 规划器；③触发器改释放边沿；④增量重规划。
- 追"数据量小怎么保证可信？"→ 定位是可行性验证不是刷 SOTA；数据量小+单一正是 shortcut learning 的温床，我诊断出 grounding 失效就是诚实面对数据局限的结果。

#### 考察什么 / 陷阱扣分

**考察什么**：①诚实（不编数字） ②分层诚实（链路通/配置就绪/成功率待补） ③主动暴露局限 ④定位清晰（可行性验证不是 SOTA）

**陷阱/扣分**：❌ 编数字（70%/80%）→ 追问必翻车 ❌ 把"链路跑通"说成"成功率很高"→ 偷换概念 ❌ 不主动说局限 → 被问出来减分

#### 🎯 面试总结

这题考的是"诚实"。三层：第一层**不编数字**（编了追问必翻车），第二层**分层诚实**（链路通/配置就绪/成功率待补），第三层**主动暴露局限**（数据小/judge 未微调/全量重排）。第三层是分水岭——能主动说"我清楚自己的边界"，才算真懂项目的局限和定位。

---

## 一、项目整体与架构

**Q1：一句话介绍项目。**
把预训练 G0Plus 双系统 VLA 通过 LoRA 跨本体微调迁移到 Fetch 机器人，并搭建 VLM 分层规划 + 传感器触发/VLM 裁决的闭环完成判断，实现仓储补货的长程移动操作。

**Q2🔥：为什么用分层架构，不用端到端长程 VLA？**
长程任务跨千步、分钟级，端到端难学难泛化。分层后复合任务的"组合"交给 VLM 零样本完成，只有原子操作技能才需要数据训练 VLA。加新复合任务只改提示词，不重训 VLA。

**Q3：System 1 / System 2 分别是什么？**
System 1 = VLA 执行器（快、高频、图像+指令+状态→动作块）；System 2 = VLM 规划器（慢、低频、指令→原子子任务）。

---

## 二、G0 双系统原理（arXiv 2509.00576）

**Q4：双系统怎么运行？频率？** Figure 1 频率金字塔：200Hz 底层控制 / 15Hz System1 VLA 出 action chunk / 2Hz System2 VLM 出子任务。两系统**异步**：System2 持续 2Hz 重规划"下一个子任务"，System1 在当前子任务下 15Hz 执行。

**Q5：两个系统同一个模型吗？怎么训练？** 不是，分开训练。System2 = Qwen2.5-VL 指令微调；System1 = PaliGemma backbone + flow matching action expert，三阶段训练。

**Q6：System2 输入输出？怎么判断完成？** 输入头部图 + 隔 1s 的 k 帧历史观测/动作 + 任务名（不喂手部相机/底层状态）；输出子任务语言。完成判断隐含在"持续决定下一个子任务"里——训练时给"子任务将结束/夹爪变化"的 key frame 更高采样权重，学会识别转换点。

**Q7：System2 训练数据怎么来？** §4.5 用 DeepSeek-R1 反向合成：喂任务名+历史/当前/下一子任务，让 R1 推理出"人类会怎么说"+"机器人怎么回应"。

**Q8🔥⚠️：通用 VLM API 当 System2 vs G0 微调版差距？** Table 1：通用模型当规划器准确率仅 15~55%，微调后 G0-VLM 到 75~83%。我的场景固定（地图/物品给定），通用 VLM 靠常识够用，但理想也该领域微调——工程成本权衡。

---

## 三、VLA 模型细节（System 1）

**Q9：动作怎么生成？为什么 flow matching 不用回归？** VLM backbone 编码图像+指令+本体→KV cache；action expert 以 KV cache 为条件，从噪声 flow matching 积分出连续动作块。flow/扩散能建模多模态动作分布（同一状态多种合理动作），回归会模式平均。

**Q10：VLA 有历史帧吗？** 没有时间历史（obs_size=1, past_action_size=0）。输入 3 路相机当前帧+当前状态+指令；时间连贯靠输出端 action chunk（一次预测 horizon 步未来动作）。`pixel_values` 的 T 维装的是 3 路相机不是 3 个时间步。

**Q11🔥：VLA 内部也有 VLM，为什么不让它自判完成？** 三点：①没有输出口（只输出动作块，无 termination 通道）；②没被训过（无完成标签）；③看不到判断所需信息（单帧+手腕特写，判断任务级完成需场景级+时序，那是 System2 的输入）。更深：让生成者给自己打完成分=generator 当自己的 verifier，校准差、有利益冲突。独立判断者信号更干净——generator/verifier 分离。

**Q12🔥：指令执行期间不变，VLA 怎么往下做？** 指令是目标不是单步命令。每次推理图像/状态在变（伸手→抓→抬→移→放），指令固定动作照样推进。VLA 学的是"给定指令整条轨迹长什么样"。

---

## 四、跨本体微调

**Q13：26-DoF→15-DoF 维度变了怎么处理？** 只边界层形状变，中间语义层全复用：ActionEncoder.linear_1 / ActionDecoder 末层 / ProprioEncoder → 全量训练；Action Mixture / Vision Tower / Gemma → LoRA；其余 ~97% 冻结。

**Q14：为什么 LoRA 不全量微调？** 小数据全量易破坏预训练知识、过拟合；LoRA 参数高效、显存友好（全量 >70G，LoRA 在 40G 可行）。rank16/alpha32，lr 5e-4。

**Q15：归一化统计量？** use_pretrained_norm_stats=False，从 Fetch 数据重算——动作分布不同必须重估。

**Q16：coarse_task / task？** G0 训练吃双指令 `[High]:{coarse_task},[Low]:{task}`。部署时总指令作 coarse_task、当前子任务作 task 透传，对齐训练分布。

---

## 五、分层部署与 VLM 规划

**Q17：VLM 怎么把指令变子任务？** 文本地图（ASCII 网格+图例+waypoint 清单）+指令 → JSON 子任务序列（type/instruction/target）→ `_parse` 校验转 Subtask。

**Q18🔥：坐标怎么来的？让 VLM 看图算吗？** 不让 VLM 从像素估坐标（最弱、误差 >0.5m 会撞架）。改成从地图 waypoint 清单"选"坐标：网格给拓扑，清单给厘米级坐标。VLM 做语义匹配+路径排序，精度押地图标定不押模型像素感知。

**Q19：导航为什么不用 VLA？** 导航是局部规划器（NavDP/iPlanner）几何控制，无学习成分、不训练。navigate_to 分段直线 + turn_to 到达后转向。

**Q20：receding horizon？为什么每 5 步重推理？** VLA 出 16/32 步 chunk 只执行前 5 步就重推理（replan_steps=5），用最新观测纠偏避免开环累积误差。导航/转向每步闭环（n_exec=1）。

---

## 六、闭环完成判断（核心亮点）

**Q21：怎么判断完成？sim vs 真机？** 仿真有 ground-truth done；真机没有，必须自己估——这是 sim2real 核心 gap。真机用传感器触发器+VLM 裁决。

**Q22🔥：详述触发器+judge 状态机。** 传感器触发（夹爪/到位/yaw）→ VLM 看头部前后帧 → **verifier 只判三态**：IN_PROGRESS（不改+刷新基准帧继续+cooldown）/ SUCCESS（前进下一子任务）/ FAIL（失败）。**verifier 职责单一，只回答"完成没有"**；失败后是重试同子任务还是重新规划，由**控制器层**根据"世界是否破坏剩余计划前提"决定（见 Q23），不塞进 verifier。

**Q23🔥：何时重规划何时只重试？** 决定的不是"成没成功"，是"世界还符不符合剩余计划假设"。抓空但物品还在→RETRY（重规划是浪费）；物品掉地上→REPLAN（前提被破坏，需插 pick_from_floor）。

**Q24：为什么事件触发不用 G0 的 2Hz？** G0 本地模型 2Hz 免费；我用 API 固定轮询慢且烧钱、中途 99% 返回"还在做"是浪费。事件触发省调用低延迟，本质相同。

**Q25：cooldown？** 判 IN_PROGRESS 后静默 N 步再问，否则退化成高频 API 轮询。

**Q26：judge 喂什么图？** 头部相机动作前后两帧对比（场景级，能看物品到位）。手部特写看不到全局不适合判进度；想确认抓取可加一帧手部看夹爪。

**Q27：judge 失败怎么办？** 降级返回 IN_PROGRESS——宁继续不误判。

---

## 七、数据与评估

**Q28：训练用多少数据？** 3 个 VLA 技能（导航不训）：pick_to_basket ~90 / restock ~90 / pick_from_floor ~60，共 ~240 条。锚点：论文 post-training 每任务 ≤100。

**Q29🔥：同量数据单物品堆量 vs 多物品少量？** 多物品少量。1物品×300背轨迹换物品崩；3物品×100同量但泛化。优先铺多样性。

**Q30：验证集？** val_set_proportion=0.05 自动按 episode 留。但 val loss 只当训练没崩哨兵，不选模型——模仿学习 loss 与成功率相关弱。

**Q31🔥：靠什么判好坏？为什么必须闭环评估？** 开环比动作 MSE 会累积误差，val loss 低≠抓得起。闭环=放进仿真真跑数成功率，唯一可信。闭环不预生成轨迹，只生成 test 场景现场 rollout，跑 20~30 episode，必须用 test 场景（未见布局）否则虚高。

**Q32🔥：闭环完成判断 vs 闭环评估一回事吗？** 不是。完成判断=部署时实时切子任务（运行时）；闭环评估=离线数成功率（开发时）。

**Q33：复合任务要训练数据吗？** 不要。VLM 零样本组合，RBM 里复合任务也只评估不生成训练数据。只训原子技能。

**Q34：240 条在 A100 40G 训多久？** ~12~20h（中位 16h）。决定因素是总帧数不是轨迹数。显存紧降 batch 升 grad_accum；想快先跑 5 epoch 看闭环。

---

## 八、局限与改进（主动暴露=加分）

**Q35⚠️：最大局限？** judge 是通用 API 未微调；pick 触发器用夹爪闭合是中间事件非释放边沿（靠 judge 兜底）；重规划是全量重排非增量；Fetch 单腕相机两路复用同图。

**Q36：更多资源下一步？** ①机器人数据 SFT 规划器；②触发器改释放边沿；③跑完整闭环评估报 train/test/OOD；④增量重规划。

---

## 九、其他方面：ML/DL 基础

**Q37：过拟合怎么判断、怎么缓解？** 训练 loss 降但验证 loss 升/成功率不涨。缓解：更多/更多样数据、正则（weight decay/dropout）、early stop、LoRA 限制可训参数、数据增强。本项目靠多物品多场景 + 冻结 97% 参数抗过拟合。

**Q38：batch size 与学习率的关系？** 大 batch 梯度估计更稳可配更大 lr（常用线性缩放）。本项目 batch4+grad_accum2=有效8；显存不够时降 batch 升 grad_accum 保持有效 batch 不变。

**Q39：梯度累积原理？** 多个 micro-batch 的梯度累加后再 step，等效大 batch 但峰值显存只占一个 micro-batch。代价是前后向次数不变、速度不提升。

**Q40：bf16/混合精度？** bf16 指数位同 fp32、动态范围大不易溢出，精度位少；省显存提速，主流大模型训练默认。注意 loss scaling（fp16 才需要，bf16 通常不需）。

**Q41：归一化（z-score）为什么重要？** 不同维度量纲差异大时，不归一化会让大量纲维度主导 loss。动作/状态 z-score 后各维同尺度，训练更稳。跨本体必须用目标本体数据重算。

---

## 十、其他方面：Transformer 与注意力

**Q42：自注意力复杂度？长序列怎么办？** O(n²) 序列长度。优化：FlashAttention（IO 感知、不省复杂度省显存带宽）、稀疏/线性注意力、滑窗。VLA 里图像 token 多，相机数×patch 数决定序列长度。

**Q43：KV cache 是什么？本项目哪里用到？** 自回归推理缓存历史 K/V 避免重复计算。G0 里 action expert 以 VLM 的 KV cache 为条件——VLM 算一次，action expert 多步去噪都复用，省算力。

**Q44：causal mask 在本项目的作用？** galaxea_zero 里 build_causal_mask 控制 vlm/proprio/action 三段 token 的可见关系，保证 action token 能看到 vlm+proprio 条件。

**Q45：位置编码？** 三段 token（vlm/proprio/action）各有 position_ids。RoPE/绝对编码各有取舍，VLA 里关键是区分模态段而非纯序列位置。

---

## 十一、其他方面：扩散与流匹配

**Q46🔥：flow matching 和 diffusion 的关系与区别？** 都从噪声生成目标。Diffusion 学逐步去噪的得分；flow matching 学一条从噪声到数据的概率流 ODE 的速度场，训练目标是回归速度向量。flow 路径更直、推理积分步数更少、更稳。

**Q47：为什么动作生成适合用 flow/diffusion 而非回归？** 动作分布多模态（同一状态多种合理动作），回归 MSE 会取平均落到不可行的中间值；生成式建模整个分布，能采样出某一个合理模态。

**Q48：psi_t / 插值在代码里？** galaxea_zero 里 x0=噪声、x1=动作、psi_t 是 t 时刻插值 `τ·x1+(1-τ)·x0`，模型预测从插值点指向数据的速度场，flow matching loss 监督它。

**Q49：推理时怎么从噪声得到动作？** 从随机噪声出发，沿学到的速度场积分若干步（欧拉等）到 t=1 得到动作块。步数是速度/质量权衡。

**Q50：action_expert_adaptive_mode / time_cond？** 时间步 t 通过 time_embedding 注入，告诉网络当前在去噪/积分的哪一步。adaptive 模式下用 AdaLN 类机制调制，否则拼接到输入。

---

## 十二、其他方面：多模态 / VLM

**Q51：SigLIP/CLIP 怎么把图像变 token？** 图像分 patch → 视觉编码器 → patch embedding 序列，经投影层对齐到语言 token 维度，与文本 token 一起进 Transformer。G0 用 SigLIP + 单层 MLP projector。

**Q52：PaliGemma 是什么？为什么 VLA 选它当 backbone？** SigLIP 视觉编码器 + Gemma 语言模型的开源 VLM。适合做 VLA backbone：体量适中、视觉-语言对齐好、易接 action expert。

**Q53：多模态怎么融合？early/late fusion？** VLA 是 early fusion——图像 token + 文本 token + 状态 token 拼成一个序列进同一个 Transformer 做注意力，而非各自编码后末端拼接。

**Q54：指令怎么影响动作？** 文本 token 与图像 token 在 backbone 里注意力交互，action expert 读这个融合表征（KV cache）条件生成。所以换指令=换条件=换动作分布。

---

## 十三、其他方面：模仿学习与 RL

**Q55🔥：这是模仿学习还是 RL？区别？** 模仿学习（behavior cloning）——从专家演示监督学动作，无奖励、无环境交互。RL 靠奖励试错。BC 简单稳定但有分布漂移问题（见下）。

**Q56🔥：BC 的分布漂移/复合误差怎么缓解？** 测试时策略自己的动作把状态带到训练没见过的分布，误差累积。缓解：receding horizon 用新观测纠偏、更多覆盖性数据、DAgger（交互式补数据）。本项目用 receding horizon + 闭环判断纠偏。

**Q57：为什么不用 RL？** 真机 RL 样本成本高、不安全；操作任务有大量演示数据，BC 性价比高。VLA 主流是大规模 BC 预训练。

**Q58：action chunking 为什么有用？** 一次预测多步未来动作，减少决策频率、缓解复合误差、动作更平滑连贯（ACT/pi0 的关键设计）。

---

## 十四、其他方面：VLA 领域横向对比

**Q59🔥：知道哪些 VLA 工作？G0 和它们什么关系？** RT-1/RT-2（Google，token 化动作）、OpenVLA（开源、自回归离散动作）、pi0（流匹配 action expert）、pi0.5、Octo（小型、扩散头）、Helix/GR00T（双系统）。G0 = pi0 式 flow matching 执行器 + 双系统规划，对标 Helix/GR00T 的快慢分层。

**Q60：自回归离散动作 vs 流匹配连续动作，优劣？** 离散（FAST tokenizer + 交叉熵）：复用 LM 训练范式、易和语言统一，但量化损失、长 chunk token 多。连续（flow matching）：动作精度高、多模态，但需额外 action expert。G0 Stage1 用离散预训练 VLM，Stage2 换流匹配 action expert——两者都用上了。

**Q61：双系统（Helix/G0）vs 单体 VLA，取舍？** 双系统：高层慢推理+低层快控制，长程任务强，但要协调两个模型。单体：简单统一，但长程规划弱。

**Q62：pi0 和 G0 的核心相似点？** 都是 VLM backbone + flow matching action expert + action chunking + 共享注意力/KV cache。G0 在其上加了双系统规划和大规模开放世界数据集。

---

## 十五、其他方面：LoRA / PEFT 深挖

**Q63🔥：LoRA 原理？为什么有效？** 冻结原权重 W，加低秩旁路 W+BA（B、A 低秩）。假设微调的权重更新本身是低秩的，所以少量参数够用。推理可合并回 W 无额外延迟。

**Q64：rank 和 alpha 怎么选？** rank 越大容量越强但越易过拟合/越费显存；alpha 是缩放（有效缩放 alpha/rank）。本项目 rank16/alpha32（缩放=2）是常见配置。

**Q65：哪些层加 LoRA？** 通常注意力的 q/k/v/o 投影；本项目还覆盖 Vision Tower 和 Action Mixture。维度变化的边界层不能用 LoRA（形状对不上），必须全量训练。

**Q66：LoRA vs 全量 vs adapter/prefix？** LoRA 无推理延迟、可合并；adapter 加层有延迟；prefix/prompt tuning 改输入不改权重、容量更小。LoRA 是当前主流。

**Q67：vlm_lr_multiplier 0.5 为什么？** 给 VLM 部分更小学习率，保护预训练视觉-语言知识，让动作相关层学得更激进些。

---

## 十六、其他方面：机器人学基础

**Q68：自由度（DoF）指什么？Fetch 15-DoF 怎么分？** 独立可控关节数。Fetch：base(3) + torso_lift(1) + arm(7) + head(2) + gripper(2)。R1Lite 26-DoF（双臂+躯干+底盘）。

**Q69：动作空间 pd_joint_pos 是什么？** 位置控制——动作=目标关节位置，底层 PD 控制器跟踪。区别于速度/力矩控制。Fetch 数据用绝对关节位置，所以无需相对变换。

**Q70：本体感知 proprioception 指什么？** 机器人自身状态（关节角、夹爪开合、底盘位姿），区别于外部感知（相机）。VLA 同时吃图像和 proprio。

**Q71：SE(2)/SE(3) 位姿？导航 waypoint 的 (x,y,yaw)？** SE(2)=平面位姿(x,y,yaw)，SE(3)=空间位姿(平移+旋转)。导航在 SE(2) 平面，waypoint 是 (x,y) + 朝向角。

**Q72：四元数 vs 欧拉角？** 欧拉角直观但有万向锁、插值不连续；四元数无奇异、插值平滑，机器人/图形学常用。turn_to 用 yaw 角（平面单自由度，欧拉够用）。

**Q73：什么是闭环控制 vs 开环？** 闭环用反馈持续纠偏（导航/转向每步读位姿）；开环按预定序列执行不看反馈。本项目 VLA receding horizon 是闭环，完成判断从开环升级到 VLM 闭环。

---

## 十七、行为面试与项目软问题

**Q74：这个项目你最大的收获/难点？** 收获：理解了双系统 VLA 的 generator/verifier 分离思想，以及 sim2real 在"完成判断"上的具体 gap。难点：搞清楚 G0 哪些是论文设计、哪些是我的简化（如事件触发 vs 2Hz），并诚实标注。

**Q75：哪个设计决策你最有信心？** waypoint 选取替代像素估坐标——把精度押在地图标定而非模型最弱的空间感知，是想清楚了模型能力边界后的决策。

**Q76：如果重做会改什么？** 先把闭环评估跑出真实成功率再写简历；规划器用领域数据微调而非通用 API；触发器一开始就用释放边沿而非夹爪闭合。

**Q77：怎么验证你的改动是对的？** 编译 + 端到端 sim 测试（如 coarse_task 透传、verifier 三态 IN_PROGRESS/SUCCESS/FAIL 的状态转移都写了测试验证），而非只看代码。

**Q78：这个项目和岗位的关系？** 覆盖了具身智能/VLA 的完整链路：预训练模型迁移、多模态、生成式动作、分层决策、sim2real——能快速上手 VLA 相关研发。

**Q79🔥：诚实题——项目跑通了吗？成功率多少？** 如实说：架构链路端到端跑通（编译+sim 验证），跨本体微调配置就绪；闭环成功率还在评估中/待补。不编数字——编了追问必翻车。

---

## 附录：考前速查（最该练的 8 题）

| # | 题 | 一句话钥匙 |
|---|---|----------|
| Q11 | VLA 为何不能自判完成 | 无输出口/没训过/看不到时序，generator≠verifier |
| Q18 | 坐标怎么来 | 从地图清单选，不从像素估，精度押地图 |
| Q23 | 重试 vs 重规划 | 看世界是否破坏剩余计划，不是看成没成功 |
| Q31 | 为何必须闭环评估 | 开环误差累积，val loss≠成功率 |
| Q46 | flow vs diffusion | 学速度场 vs 学得分，flow 路径直步数少 |
| Q55 | IL vs RL + 分布漂移 | BC 学演示，receding horizon 纠偏 |
| Q59 | VLA 横向 | pi0/OpenVLA/Helix，G0=流匹配+双系统 |
| Q79 | 诚实题 | 链路通，成功率待补，不编数字 |

> 提示：被问到不确定的，先讲清"代码证实的"和"论文/推测的"边界，比硬答更显专业。

---

## 十八、其他方面：经典 CV 模型与基础

**Q80：CNN 为什么适合图像？卷积的归纳偏置？** 局部连接（局部感受野）+ 权值共享（平移不变）+ 层级特征。归纳偏置=局部性和平移等变，小数据比 ViT 更省样本。

**Q81：CNN vs ViT 区别与取舍？** CNN 有强归纳偏置、小数据好；ViT 全局注意力、大数据上限高但需更多数据/正则。VLA 的视觉编码器（SigLIP）是 ViT 系。

**Q82：ResNet 残差连接解决什么？** 梯度消失/退化问题，让深层网络可训；恒等映射使梯度直通。

**Q83：BatchNorm vs LayerNorm？为什么 Transformer 用 LN？** BN 跨 batch 统计，依赖 batch size、序列任务不稳；LN 在单样本特征维归一，与 batch 无关，适合变长序列/小 batch。Transformer 用 LN。

**Q84：目标检测两阶段 vs 一阶段？** 两阶段（Faster R-CNN）先选 proposal 再分类，准但慢；一阶段（YOLO/SSD）直接回归，快。本项目导航避障若用感知会涉及。

**Q85：图像分割语义/实例/全景区别？** 语义=每像素类别（不分个体）；实例=区分个体；全景=两者合并。

---

## 十九、其他方面：经典 NLP / LLM 基础

**Q86：word2vec / 词向量原理？** 用上下文预测词（CBOW）或词预测上下文（skip-gram），学出语义相近词向量相近。是早期分布式表示。

**Q87：BERT vs GPT？** BERT 双向编码器、masked LM、适合理解任务；GPT 单向解码器、自回归、适合生成。VLA 的语言 backbone（Gemma）是 GPT 式解码器。

**Q88：什么是 tokenizer？BPE？** 把文本切成子词单元。BPE 按频率合并字符对，平衡词表大小和 OOV。G0 Stage1 还用 FAST tokenizer 把连续动作离散化。

**Q89🔥：LLM 的 SFT / RLHF / DPO 区别？** SFT=监督微调学指令跟随；RLHF=用人类偏好训奖励模型再 RL（PPO）；DPO=跳过奖励模型直接用偏好数据优化，更简单稳定。G0-VLM 用的是 SFT。

**Q90：温度 temperature / top-p 采样？** 温度调分布尖锐度（低=确定，高=多样）；top-p 从累积概率 p 的核内采样。本项目 VLM 规划用低温（0.1）求确定性。

**Q91：幻觉 hallucination 怎么缓解？** RAG 检索增强、约束输出格式、降温、要求引用。本项目让 VLM 从 waypoint 清单"选"而非"编"坐标，就是约束输出抗幻觉。

---

## 二十、其他方面：优化与训练

**Q92：SGD / Adam / AdamW 区别？** SGD+动量简单泛化好；Adam 自适应学习率收敛快；AdamW 把权重衰减和梯度更新解耦，是 Transformer 主流。本项目用 AdamW。

**Q93🔥：学习率 warmup + cosine 为什么？** warmup 避免初期大梯度震荡（尤其大模型/大 batch）；cosine 衰减后期精调。本项目 warmup_steps=500 + cosine。

**Q94：梯度裁剪作用？** 防梯度爆炸，裁剪范数上限，训练更稳。

**Q95：weight decay / L2 正则？** 惩罚大权重防过拟合，提升泛化。本项目 1e-2。

**Q96：梯度消失/爆炸成因与对策？** 深层链式连乘。对策：残差、归一化、合理初始化、梯度裁剪、ReLU 系激活。

**Q97：常见激活函数？为什么用 GELU/SiLU？** ReLU 简单但死神经元；GELU/SiLU 平滑、Transformer 常用。

---

## 二十一、其他方面：评估指标

**Q98：精确率/召回率/F1？什么时候看哪个？** Precision=预测正里真正比例；Recall=真正里被找出比例；F1 调和平均。漏检代价高看 Recall，误报代价高看 Precision。

**Q99：mAP / IoU（检测分割）？** IoU=交并比衡量框/掩码重叠；mAP=各类 AP 平均，检测主指标。

**Q100🔥：本项目为什么用成功率不用 loss？（呼应 Q31）** 模仿学习 loss 衡量动作拟合，但误差累积、loss 低≠任务成。成功率是任务级闭环指标，唯一可信。

**Q101：ROC-AUC vs PR-AUC？** AUC 衡量排序能力；类别不平衡时 PR 曲线更敏感。

---

## 二十二、其他方面：工程与系统

**Q102：模型部署优化手段？** 量化（int8/fp16）、蒸馏、剪枝、算子融合、KV cache、batching。VLA 真机要满足实时性（15Hz）。

**Q103：训练慢/显存不够怎么排查优化？** profile 找瓶颈；混合精度、梯度累积、梯度检查点、ZeRO/FSDP 分片、LoRA 减可训参数。

**Q104：分布式训练 DP vs DDP vs FSDP？** DP 单进程多卡（慢）；DDP 多进程各持完整模型梯度同步；FSDP/ZeRO 分片参数/优化器状态省显存，训大模型用。

**Q105：数据加载是瓶颈怎么办？** 多 worker、预取、缓存解码、用高效格式（LeRobot/视频编码）。本项目数据存 mp4 + parquet。
